{
	"pluginAlias": "Abode Lights",
	"pluginType": "platform",
	"singular": true,
	"headerDisplay": "Enter the login credentials for your Abode account.",
	"schema": {
		"type": "object",
		"properties": {
			"email": {
				"title": "Email",
				"type": "string",
				"description": "Email registered with Abode",
				"required": true
			},
			"password": {
				"title": "Password",
				"type": "string",
				"required": true
			},
			"pollingInterval": {
				"title": "Polling Interval",
				"description": "(Optional) Sets the frequency (in minutes) for which to poll Abode for device updates. If left blank, polling is disabled.",
				"type": "number",
				"required": false,
				"minimum": 5
			}
		}
	}
}